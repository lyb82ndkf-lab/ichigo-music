# 更新日志 (Changelog)

All notable changes to **ICHIGOMusic** will be documented in this file.

---
## [2.9.0] - 2026-09-07

### 🛠️ 缓存系统与网络健壮性全面升级

- **修复封面与音频缓存网络异常**：
  - 彻底修复音乐播放过程中 `cache-cover` 报错 `TypeError: terminated (node:internal/deps/undici/undici)` 的问题。
  - 主进程网络请求全面切换至 Electron 原生 Chromium 网络栈（`electronNet.fetch`），支持更强大的 HTTP/HTTPS 连接池管理与自动重连。
  - 为封面（20s）与音频（60s）缓存下载增加超时机制与完整的异常兜底保护，网络抖动或连接被 CDN 中断时平滑降级，绝不打断音乐播放与切歌。
  - 强化 Windows 下的写入流关闭与临时文件清理逻辑，杜绝文件句柄占用引发的残留报错。

---

### 🔊 音频流路由与 Web Audio CORS 零声音问题修复

- **彻底解决“进度条在走但没有声音” (CORS access restrictions) 故障**：
  - 深入排查并修复控制台报错 `MediaElementAudioSource outputs zeroes due to CORS access restrictions`。
  - **Web Audio 节点解绑与原生直出**：
    - 查明 Web Audio API 会永久劫持 `<audio>` 元素声音输出并因无 CORS 授权强制静音输出全 0 的底层行为。
    - 为 `<audio>` 标签绑定 `key={audioRoutingMode}` 响应式重挂载机制；在切换到直出模式时立即解绑老节点并挂载全新原生音频 DOM。
    - 在 `setupWebAudio()` 中增加路由守护，直连模式下坚决阻断 `createMediaElementSource` 介入，使音频直通操作系统声卡硬件。
  - **智能缓冲识别与超时放宽**：
    - 修复大体积无损 FLAC 音频在首次建立 TLS 连接与分块传输时因 1.2s 超时被误判为卡死并错误切断代理的逻辑。
    - 引入 `audio.networkState === 2`（正在缓冲）动态识别，将判定窗口合理调整至 5s～8s，确保大码率音质从容加载。
  - **本地音频代理完善**：
    - 主进程音频代理增加标准 HTTP `OPTIONS` 跨域预检响应（204 No Content 并附带完整 CORS 授权头）。
    - 增加对 `HEAD` 试探请求的识别与原样透传，强化 `Range` 范围流分段传输的稳定性。
---


## [2.8.0] - 2026-09-05

### 💖 心动模式重构与智能播放体验升级

- **真随机动态混洗算法**：
  - 修复此前心动模式会将原歌单一次性打乱并按顺序机械推进的“伪随机”问题。
  - 现启动心动模式后，**播放队列完整保持原有歌单的自然顺序与完整结构**，核心播放引擎在底层维护动态候选池执行**真随机选择**。
  - 支持单周期无重复播放机制，当本轮基础歌曲全部播放完毕后自动无缝重置候选池，保证曲目分布均匀自然。

- **彻底修复“1-2-6-1”循环复读与跳回开头问题**：
  - 修复穿插推荐未红心好歌时将曲目追加至播放列表末尾、导致下一首取模越界跳回第 1 首的严重逻辑 Bug。
  - 智能推荐歌曲现以平滑插入方式**就地置于当前播放曲目正后方**，推荐歌曲播放完毕后，下一首立即从剩余未播放基础歌曲中真随机抽取，顺畅连续不中断。
  - 增强 `playSong` 队列兜底机制，任意独立歌曲动态插入时均就近插入在当前曲目后方，防止列表错位。

- **完善双向播放历史栈**：
  - 为随机播放模式与心动模式引入独立的播放历史追踪栈（`playbackHistory`）。
  - 支持在心动推荐或随机切歌过程中点击“上一首”精准回退已播歌曲（例如 `3 -> 6 -> 2 -> 1`）。
  - 回退后再点“下一首”可沿历史序列顺畅前进，历史记录末端继续触发真随机探索。

---

### 🎨 界面与主题交互优化

- **我喜欢的音乐 / 歌单详情页心动模式 UI 全面重构**：
  - 提取并封装全新自适应胶囊按钮 `.heart-mode-pill-btn`，彻底替代原固定内联样式。
  - **深色模式深度适配**：微透暗色毛玻璃基底搭配半透明粉彩细边框与呼吸阴影，主标题高亮通透，心跳图标与主题深度呼应。
  - **浅色模式完美兼容**：适配洁净高质感浅色玻璃面板与深色高对比文字（`#1f2937`），图标采用高饱和度品红色，彻底解决浅色背景下文字发灰发白、无法看清的问题。
  - **交互动效与状态指示**：激活状态应用专属双端渐变高亮背景与纯白心跳动效，并右侧附带“播放中”状态胶囊微标。
  - **一键切换/退出机制**：已处于心动模式时，再次点击该按钮可直接一键退出心动模式并恢复原列表顺序循环播放。

---

### 🛠️ 工程化、质量与构建发布

- **自动化验证用例集成**：
  - 新增 `check:heart-playback` 专项测试套件（`scripts/check-heart-mode-playback.mjs`），覆盖初始列表保护、智能推荐就地插入、防首曲跳回与历史栈倒退等全套核心路径。
  - 已将该检查集成至全局 `npm run check` 门禁中。
- **发布产物生成**：
  - 正式构建 Windows 平台桌面端版本：
    - 安装程序：`release/ICHIGOMusic Setup 2.8.0.exe`
    - 免安装绿色解压版：`release/win-unpacked/`
